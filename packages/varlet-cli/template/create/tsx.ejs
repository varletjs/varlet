import { defineComponent } from 'vue'
import './<%- name %>.less'

export default defineComponent({
  setup(props, { slots }) {
    return () => {
      return <div>{ slots.default?.() }</div>
    }
  },
})
